intsall Node and NPM first, we dont need node directly, we just need the angular dev tool , npm 

install angular CLI globally  >   npm install @angular/cli --g
g == that command gives ng command

create a new project   >   ng new app-name-goes-here (ng new wishlist)
answers to optional questions > rorting = default NO, styles = CSS
cd into that newe folder

to start app (first time) , in cmd > 1. ng serve 
if for the first time after cloned into a new pc> first do npm install

no definite structure for folder structure

material UI is from Google

##################### CREATING A DATA MODAL #######################
no definite structure for folder structure

classes will be made inside shared/modules bcz they will be used in many places inside the app

##################### install bootstrap####################
1. in root folder> npm install bootstrap
2. verify that the bootstrap folder is exist
3. add followinh under 'build' section of angular.json file >>>
    "styles": [
              "node_modules/bootstrap/dist/css/bootstrap.min.css",
              "src/styles.css"
            ],
            "scripts": [
              "node_modules/bootstrap/dist/js/bootstrap.bundle.min.js"
            ]
4. stop server and start again > ng serve 


################### for loops in NG ###################
instead of manually displaying all the items in an array, use a angular for loop
type '*' inside a div we need to start
works same as attributes > *ngFor=""
its called a directive, specially a STRUCTUAL DIRECTIVE = it changes the str of DOM 
remember to import > import { CommonModule } from '@angular/common';  in app.ts 
and inside @Component >  imports: [CommonModule,RouterOutlet],

as of 2025, using ngFor is deprecated 
instead use @for  (continuing these syntaxes from now)

if we commented the items in app.ts this will give an error (then its an array of 'never' objects)

in new version when we use @if we dont need to use <ng-template>   (37.00)

we cant have 2 structual directive on the same element(inside same <>)

@########################bind data@#########################
<input type="checkbox" class="checkbox" [checked]="wish.isDone"> 
this wont show in the DOM as 'checked' , but show in UI

adding a data index attribute > same way,but use a prefix [attr.attr name]s
 [attr.data-index]="ii">

@########################bind events@#########################
<input (click)="toggleItem()" ,,,,
the function will be written in app.ts... 
we can add a function in the same line of html

@########################forms@#########################
2 diff wways to work with
1. reactive forms = more scalable, but need setup
2. template driven forms (in this lesson)

ngmodal = a field that we can bind to a property of our class(component)
input tag's name attribute must have a value
import { FormsModule } from '@angular/forms'; in app.ts and inside Component> imports: [CommonModule, RouterOutlet, FormsModule],


########################creating a component with input@#########################
1.20.00
ng generate component name-here
ng generate component wish-list-comp

remove 'app' from >
 @Component({
  selector: 'app-wish-list-comp', 
  
  in wish-list.comp.ts





